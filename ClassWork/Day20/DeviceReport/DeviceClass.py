#!/usr/bin/python

class DeviceReport(object):
    def __init__(self, device_id, device_name, device_type, device_is_powered, device_battery_status, device_manufacturer, device_processor):
        self.device_id = device_id
        self.device_name = device_name
        self.device_type = device_type
        self.device_is_powered = device_is_powered
        self.device_battery_status = device_battery_status
        self.device_manufacturer = device_manufacturer
        self.device_processor = device_processor
    def 
